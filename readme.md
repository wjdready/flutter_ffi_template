
# How to run

```sh
# search all flutter_ffi_template and replace to your_custom_name
# and then rename lib/flutter_ffi_template.dart to your_custom_name
cd example
flutter create --platforms windows .
flutter run

# clean all ignore file to keep workspace clear
git clean -fdx
```
